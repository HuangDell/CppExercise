#include <stdio.h>
class DrawList{
    private:
        void  drawLine(int length)
        {
            for (int i=0;i<length;i++)
                printf("-");
        }
    public:
        static void drawList(vector <Ware> house)
        {
            

        }

};